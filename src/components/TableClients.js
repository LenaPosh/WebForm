import React from 'react';

const TableClients = ({ data }) => {
    // Ваш компонент с табличкой
    return (
        <div className="table">
            {/* Рендер таблички */}
        </div>
    );
};

export default TableClients;