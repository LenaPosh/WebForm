import React from 'react';

const TableCash = ({ data }) => {
    // Ваш компонент с табличкой
    return (
        <div className="table">
            {/* Рендер таблички */}
        </div>
    );
};

export default TableCash;