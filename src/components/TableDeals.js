import React from 'react';

const TableDeals = ({ data }) => {
    // Ваш компонент с табличкой
    return (
        <div className="table">
            {/* Рендер таблички */}
        </div>
    );
};

export default TableDeals;